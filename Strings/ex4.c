#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
	char str1[100], str2[100];
	gets(str1);
	gets(str2);

	for (int i = 0; str1[i] == str2[i] && i<strlen(str1) && strlen(str2); ++i);
	str[i]-
	return 0;
}